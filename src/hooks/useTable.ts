import { computed } from "vue";
import { useStore, mapState } from "vuex";

interface Result {
  getList: Function;
  api: Function;
}

const useTable = function ({ api: Function, params: object }): Result { };

export default useState;
